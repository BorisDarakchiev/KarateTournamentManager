@page
@model IndexModel
@{
    ViewData["Title"] = "Profile";
    ViewData["ActivePage"] = ManageNavPages.Index;
}

<h1>@ViewData["Title"]</h1>

<div class="alert alert-info">
    <strong>Информация:</strong> Тук можете да управлявате настройките на акаунта си.
</div>

<div class="card">
    <div class="card-header">
        <h5>Change your password</h5>
    </div>
    <div class="card-body">
        <p>To change your password, use the form below.</p>
        <a class="btn btn-primary" href="@Url.Page("/Account/Manage/ChangePassword")">Change password</a>
    </div>
</div>

@* <div class="card mt-3">
    <div class="card-header">
        <h5>Two-Factor Authentication</h5>
    </div>
    <div class="card-body">
        <p>Manage your two-factor authentication options.</p>
        <a class="btn btn-secondary" href="@Url.Page("/Account/Manage/TwoFactorAuthentication")">Manage 2FA</a>
    </div>
</div>

<div class="card mt-3">
    <div class="card-header">
        <h5>External Logins</h5>
    </div>
    <div class="card-body">
        <p>Manage your external logins, like Google or Facebook.</p>
        <a class="btn btn-secondary" href="@Url.Page("/Account/Manage/ExternalLogins")">Manage external logins</a>
    </div>
</div> *@

@* <div class="card mt-3">
    <div class="card-header">
        <h5>Delete Account</h5>
    </div>
    <div class="card-body">
        <p>Delete your account permanently. This action is irreversible.</p>
        <a class="btn btn-danger" href="@Url.Page("/Account/Manage/ConfirmDelete")">Delete your account</a>
    </div>
</div>
 *@

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
